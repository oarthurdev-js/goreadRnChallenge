import {View} from 'react-native';
import React from 'react';
import WebViewRepo from '../../components/webview';

export default function WebViewPage({navigation}) {
  return (
    <View>
      <WebViewRepo />
    </View>
  );
}
